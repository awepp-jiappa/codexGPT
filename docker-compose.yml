services:
  nas-gpt-chat:
    build: .
    container_name: nas-gpt-chat
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL:-file:/data/nas-gpt-chat.db}
    volumes:
      - ./data:/data
    restart: unless-stopped
